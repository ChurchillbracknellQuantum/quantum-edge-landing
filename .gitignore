# DB credentials live in ~/private/titan_secrets.php (outside public_html)
# config.php is a safe loader — committed to GitHub, no real credentials inside

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
